%h4{style: "margin-left: 110px; margin-top: 100px; text-align: center;"} LIST OF Menus

%div.row{style: "margin-top: 6%;"}



%table.table.table-bordered.table-striped.text-center
  %thead.table-dark
    %tr
      %th
        = sort_link(@q, :id, 'ID')
      %th
        = sort_link(@q, :name, "Name")
      %th
        = sort_link(@q, :rating, "Price")


  %tbody
    - @menu_items.each do |menu_item|
      %tr.content-row
        %td=menu_item.id
        %td
          %a{href: new_restaurant_menu_item_order_path(@restaurant, menu_item_id)}= menu_item.item_name
        %td= menu_item.price
        

%div.pagination-wrapper{style: "text-align: center;"}
  = will_paginate(@menu_items, renderer: WillPaginate::ActionView::Bootstrap4LinkRenderer)